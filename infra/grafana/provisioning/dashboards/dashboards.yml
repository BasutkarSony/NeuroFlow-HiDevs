apiVersion: 1

providers:
  - name: NeuroFlow
    orgId: 1
    folder: NeuroFlow
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
